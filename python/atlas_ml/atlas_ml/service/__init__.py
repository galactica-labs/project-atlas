"""HTTP engine: FastAPI surface over the atlas_ml reasoning pipeline.

The NestJS bridge (projects/backend/src/pipeline) proxies to this. Run with:
    uvicorn atlas_ml.service.app:app --port 8000
"""
